# Firebase → Authentication → Google → Web client ID (used by Firebase + web)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Google Cloud → Credentials → OAuth 2.0 → iOS client (separate from Web client)
# Bundle ID must match app.json: com.syncora.app
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
